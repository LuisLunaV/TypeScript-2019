"use strict";
(() => {
    var _a;
    const heroeUno = "Ironman";
    const heroeDos = 'Spider-man';
    const heroeTres = `Heroe: Namor`;
    console.log(`I'm ${heroeUno}`);
    console.log(((_a = heroeDos[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No esta presente');
})();
