"use strict";
(() => {
    const array = [1, 2, 'hola', 3, true];
    const numeros = [1, 2, 3, 4, 5];
    const palabras = ['Hola', 'mundo', ':)'];
    array.forEach(info => {
        console.log(info);
    });
    // numeros.forEach( numero => {
    //     console.log(numero)
    // });
    // palabras.forEach( palabras => {
    //     console.log(palabras)
    // });
})();
